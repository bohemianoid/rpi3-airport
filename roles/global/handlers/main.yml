---
- name: reboot
  shell: sleep 2 && reboot
  become: yes
  async: 1
  poll: 0
  ignore_errors: true

- name: wait for reboot
  local_action:
    module: wait_for
    host: "{{ ansible_host }}"
    port: 22
